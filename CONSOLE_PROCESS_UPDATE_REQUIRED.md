# 🚨 ТРЕБУЕТСЯ ОБНОВИТЬ ПРОЦЕСС В SHARETRIBE CONSOLE

## ❌ ТЕКУЩАЯ ПРОБЛЕМА

**403 Forbidden** при попытке закрыть задание (transition/complete)

### 🔍 Причина:

Локальный файл `ext/transaction-processes/assignment-flow-v3/process.edn` правильный:
```clojure
{:name :transition/complete,
 :actor :actor.role/provider,  ✅ ПРАВИЛЬНО
 :actions [],
 :from :state/accepted,
 :to :state/completed,
 :privileged? true}  ✅ ПРАВИЛЬНО
```

**НО** Sharetribe Console все еще использует старую версию процесса с:
```clojure
:actor :actor.role/customer  ❌ НЕПРАВИЛЬНО
```

### 📝 Как это работает:

1. **`:privileged? true`** позволяет делать transition через backend API
2. **НО** Sharetribe **ВСЁ ЕЩЁ ПРОВЕРЯЕТ** actor restriction!
3. Даже с trusted token, если `:actor :actor.role/customer`, а реальный actor - provider → **403 Forbidden**

---

## ✅ РЕШЕНИЕ: Обновить процесс в Console

### Шаг 1: Войти в Sharetribe Console

1. Откройте https://flex-console.sharetribe.com/
2. Выберите ваш marketplace: **youdoae-dev**
3. Перейдите в **Build** → **Transaction processes**

### Шаг 2: Найти и отредактировать процесс

1. Найдите процесс **`assignment-flow-v3`** в списке
2. Нажмите **Edit** (кнопка с карандашом)
3. Откроется редактор с кодом процесса

### Шаг 3: Обновить transition/complete

Найдите секцию `transition/complete` (примерно строки 19-24):

**БЫЛО (неправильно):**
```clojure
{:name :transition/complete,
 :actor :actor.role/customer,
 :actions [],
 :from :state/accepted,
 :to :state/completed}
```

**ДОЛЖНО БЫТЬ:**
```clojure
{:name :transition/complete,
 :actor :actor.role/provider,
 :actions [],
 :from :state/accepted,
 :to :state/completed,
 :privileged? true}
```

### Шаг 4: Сохранить и опубликовать

1. Нажмите **Save** (внизу редактора)
2. Нажмите **Publish** (чтобы изменения вступили в силу)
3. Подтвердите публикацию

⚠️ **ВАЖНО:** Изменения вступят в силу **сразу** после публикации!

---

## 🧪 После обновления - Тестирование

### 1. Перезапустить локальный сервер (если нужно)

```bash
# В терминале (если сервер уже запущен, не нужно)
yarn dev
```

### 2. Протестировать закрытие задания

1. Обновите страницу в браузере (Cmd+Shift+R)
2. Авторизуйтесь как **Provider (Заказчик)**
3. Откройте чат с принятым Customer (Исполнителем)
4. Нажмите кнопку **"Закрыть задание"**
5. Подтвердите действие

### ✅ Ожидаемый результат:

**В консоли браузера:**
```
🔄 onComplete: starting transition/complete for transaction ...
✅ onComplete: transition successful
```

**В консоли backend (терминал):**
```
🔍 getTrustedSdk: token exchange successful
🔍 transition-privileged: calling SDK with body: ...
✅ transition-privileged: success, status = 200
```

**В интерфейсе:**
- ✅ Задание успешно закрывается
- ✅ Страница перезагружается
- ✅ Появляется форма для оставления отзыва

---

## 📋 Что изменилось и почему

### История проблемы:

1. **Изначально:** `:actor :actor.role/customer` для `transition/complete`
2. **Поменяли роли:** Customer → Исполнитель, Provider → Заказчик
3. **Обновили локально:** `:actor :actor.role/provider` в `process.edn`
4. **НО:** Console не обновился автоматически!

### Почему локальный файл не работает:

- Локальные `.edn` файлы используются **только для разработки и тестирования**
- **Production процессы** хранятся **в Sharetribe Console**
- Даже в development mode, если процесс опубликован в Console, **Console имеет приоритет**

### Как работает privileged API:

```
Frontend (User clicks "Close task")
    ↓
Backend /api/transition-privileged
    ↓
getTrustedSdk(req) - exchanges user token → trusted token
    ↓
trustedSdk.transactions.transition()
    ↓
Sharetribe Flex API
    ↓
Проверяет: currentUser.userType === process.actor?
    ↓
Если НЕТ → 403 Forbidden
Если ДА → 200 Success
```

`:privileged? true` позволяет вызывать transition **через backend**, но **НЕ отменяет** actor restriction!

---

## 🔄 Альтернатива (если не хотите менять Console)

Если по какой-то причине вы не хотите менять процесс в Console прямо сейчас, можно временно:

1. **Вариант A:** Сделать так, чтобы Customer мог закрывать задание
   - Но это нарушит бизнес-логику (только Provider должен закрывать)

2. **Вариант B:** Использовать Integration API (требует доп. настройку)
   - Нужны специальные scope для Integration API
   - Более сложная настройка

**Рекомендация:** ✅ Обновить процесс в Console (5 минут) - самое простое и правильное решение!

---

## 📞 Если что-то не работает

1. Проверьте, что процесс **опубликован** (Published) в Console
2. Проверьте логи backend в терминале на наличие ошибок
3. Проверьте Network tab в браузере (F12 → Network → transition-privileged)
4. Убедитесь, что пользователь авторизован как Provider (userType: 'provider')

---

**После обновления Console сообщите, и я помогу с тестированием!** 🚀

